---
name: Component scaffold skill location
description: Project has a component-scaffold skill that auto-triggers on new component/page creation requests
type: project
---

A skill file exists at `.claude/skills/component-scaffold/SKILL.md` in the sons-of-mountains project.

It auto-triggers when user says: create component, new component, create page, new page, add section, scaffold, build component, make component.

**Why:** User wants every new component to automatically get full Payload CMS field definitions, real-time revalidation, individually-clickable Puck editor sections, /puck/<name> editor route, API save route syncing props back to Payload, admin Visual Editor button, frontend floating admin-only edit button.

**How to apply:** Always read the skill file first, then follow all 14 checklist steps. The footer is the canonical reference implementation.
